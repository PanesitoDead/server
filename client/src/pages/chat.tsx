import { ChatContainer } from "@/components/chat";

export default function Chat() {
  return (
    <div className="font-inter bg-chat-background h-screen flex flex-col">
      <ChatContainer />
    </div>
  );
}
